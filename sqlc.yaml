version: "2"
sql:
  - engine: "sqlite"
    schema: "internal/sqlite/migrations"
    queries: "internal/sqlite/queries"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_interface: true
